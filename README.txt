CAL Project README:

- Libraries and API's:
    + No other libraries or API's were used besides the GraphViewer API given in classes.
- Compilation instructions:
    + Since the Project were implemented on CLion, we suggested using the cmake command
